import React from 'react';

const HeroSection = () => {
    return (
        <section className="hero">
            <h1>Welcome to Depo!</h1>
            <p>Join our waitlist for exclusive access to minting tokens on Solana.</p>
        </section>
    );
};

export default HeroSection;
